System
======

soma records the configuration and artifacts behind each experiment while
reusing deterministic intermediate results across compatible runs.

* :doc:`caching` explains cache identity and reuse.
* :doc:`outputs` describes the run-directory artifact schema.
* :doc:`reporting` covers single-run reports and multi-run comparisons.

.. toctree::
   :maxdepth: 1
   :hidden:

   caching
   outputs
   reporting
