Training & evaluation
=====================

The trainable downstream model is optimized against a task-specific objective,
then evaluated with consistent metrics and prediction artifacts.

* :doc:`training` covers optimization, folds, checkpoints, and early stopping.
* :doc:`evaluation` covers metric configuration and computation.

.. toctree::
   :maxdepth: 1
   :hidden:

   training
   evaluation
