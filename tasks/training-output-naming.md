# Training Output Naming

- [x] Update training and pipeline tests to use `fold_dir` and `run_dir`.
- [x] Rename standalone training and trainer APIs away from generic `output_dir`.
- [x] Rename pipeline result metadata to `run_dir` where it represents the resolved run path.
- [x] Verify pipeline and config tests pass.
