# Tiling Run Layout

- [x] Inspect the current `FeatureExtractor.run()` and pipeline run-dir layout.
- [x] Change the run-level tiling path to be a sibling of `features/` instead of nested under it.
- [x] Add a regression test that locks in the sibling `tiling/` layout.
- [x] Update docs to describe the run-local output tree accurately.
- [x] Run targeted tests to verify the layout change.
